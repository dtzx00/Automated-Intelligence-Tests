# AUT – Alternative Uses Task

`instruct(cue=None, seed=None)` returns instructions for generating creative uses of a common object.

If `cue` is omitted, one is sampled from the standard set (brick, paperclip, …).

`evaluate` is currently a placeholder (scoring not yet implemented).
