# WRT – Creative Writing Task

`instruct(cues=None, seed=None)` returns instructions for writing a short creative story that must include three cue words.

If `cues` is omitted, a triple is sampled from the standard set.

`evaluate` is currently a placeholder (scoring not yet implemented).
