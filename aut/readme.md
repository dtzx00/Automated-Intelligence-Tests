# AUT – Alternative Uses Task

`instruct(object=None, seed=None)` returns instructions for generating creative uses of a common object.

If `object` is omitted, one is sampled from the standard set (brick, paperclip, …).

`evaluate` is currently a placeholder (scoring not yet implemented).
